export enum HomeKbn {
    HOME = '1',
    VISITOR = '2',
};

export enum SequenceKbn {
    MATCH = '1',
}

export enum GameStatus {
    CREATED = '0',
    PLAYER_REGISTERED = '1',
    FINISHED = '2',
}